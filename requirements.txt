# =============================================================================
# Cricket Match Predictor Requirements
# =============================================================================
# 
# APPLE SILICON USERS (M1/M2/M3):
# For GPU acceleration, use the dedicated Apple Silicon setup instead:
#   ./scripts/setup_apple_silicon.sh
# 
# Or manually install with:
#   pip install -r requirements-apple-silicon.txt
#
# This enables Metal GPU acceleration (~400-600 sims/sec vs ~100 sims/sec on CPU)
# =============================================================================

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# Database
sqlalchemy>=2.0.0

# Machine Learning - Baseline
scikit-learn>=1.3.0
xgboost>=2.0.0

# Deep Learning
# Note: For Apple Silicon GPU support, use requirements-apple-silicon.txt instead
# which includes tensorflow-macos + tensorflow-metal
tensorflow>=2.15.0
torch>=2.1.0
torchvision>=0.16.0
keras-tuner>=1.4.0

# Web Framework
flask>=3.0.0
flask-wtf>=1.2.0
flask-cors>=4.0.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# Jupyter Notebooks
jupyter>=1.0.0
ipykernel>=6.27.0

# Web Scraping (for CREX)
beautifulsoup4>=4.12.0
lxml>=5.0.0
playwright>=1.40.0

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0
rapidfuzz>=3.5.0  # For fuzzy string matching

# Wave 5 Phase 6: Polymarket write-path
# py-clob-client is the official Polymarket CLOB SDK. We pin BOTH the v1 and v2
# clients: v2 (write-path) became mandatory after Polymarket's 2026-04-27 protocol
# upgrade (order v1 signatures get `order_version_mismatch`), and v1 is retained
# for the bootstrap script's --approve path that still exposes
# `create_or_derive_api_creds`. eth_account derives the wallet address from the
# private key generated by scripts/bootstrap_polymarket_wallet.py.
py-clob-client>=0.34.0
py-clob-client-v2>=1.0.0
eth-account>=0.10.0
keyring>=24.0.0  # Optional: store POLYGON_PRIVATE_KEY in macOS Keychain

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Code Quality
black>=23.12.0
flake8>=6.1.0
isort>=5.13.0

